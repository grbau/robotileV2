// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // replace this with your config
  firebaseConfig: {
    apiKey: 'AIzaSyCmeIebAEDfBHmXe6Kqcv4tnOFRf9RNoTM',
    authDomain: 'carlita-robotile.firebaseapp.com',
    //databaseURL: '#{databaseURL}',
    projectId: 'carlita-robotile',
    storageBucket: 'carlita-robotile.appspot.com',
    messagingSenderId: '19805670720',
    appId: '1:19805670720:web:fb257b5c32f5782e0cfea5',
    measurementId: 'G-0QC79REYLM',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
